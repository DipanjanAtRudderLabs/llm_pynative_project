# This is a sample file, for detailed reference see: https://rudderlabs.github.io/pywht/
inputs:
  - name: rsMainIdStitchedFeatures
    contract:
      is_optional: false
      is_event_stream: true
      with_entity_ids:
        - user
      with_columns:
        - name: user_main_id
        - name: first_name
    app_defaults:
      table: data_apps_simulated_shopify.shopify_user_features
